rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Hver bruker har kun tilgang til sine egne data.
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
